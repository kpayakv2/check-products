# 05: ชะตากรรมของ START_HERE.md

**Labels:** `wayfinder:grilling`
**Parent:** [แผนที่](../map.md)
**Blocked by:** ~~[01: ลำดับศักดิ์ของแหล่งความจริง](01-precedence-order.md)~~ — ปิดแล้ว 2026-09-06 ตั๋วนี้ไม่ติดบล็อกแล้ว
**Assignee:** (ว่าง — ยังไม่มีใครจอง)
**Status:** open

## Question

`START_HERE.md` เป็นเอกสาร **"อ่านนี้ก่อน"** ที่ระดับราก แต่แทบทุกอย่างในนั้นตายแล้ว — จะ **ลบทิ้ง / เขียนใหม่ให้เป็นทางเข้าจริง / ยุบรวมกับ README.md** และถ้ามีทางเข้าเดียว ทางเข้านั้นควรเป็นไฟล์ไหน

## สภาพจริง (ยืนยันแล้ว 2026-09-05)

- อ้างเอกสารต่อ 6 ไฟล์ **ไม่มีเหลือสักไฟล์**: `DEPLOY_LOCAL_MODEL.md`, `STATUS_CHECK_FINAL.md`, `README_INTEGRATION.md`, `SUMMARY_ARCHITECTURE.md`, `MIGRATION_PLAN.md`, `INTEGRATION_STRATEGY.md`
- บรรทัด 85 สั่ง `python api_server.py` ที่ระดับราก — ไฟล์ย้ายไป `src/api/api_server.py` แล้ว **ขัดกฎเหล็กข้อ 1 ("`src/` structure only") ของโปรเจกต์เอง**
- บรรทัด 241 พูดถึง `web_server.py` (Flask) ที่ "ยังใช้ FastAPI เหมือนเดิม" — **ไฟล์ไม่มีอยู่**
- บรรทัด 18/199/264 ยืนยัน "Accuracy 72%" ซึ่งเป็นตัวเลขปลอมที่ `CLAUDE.md` ประกาศยกเลิกแล้ว
- บรรทัด 104/179 ให้เปิด `http://localhost:3000/import` — ต้องตรวจว่าเส้นทาง `/import` ยังมีอยู่ (`CLAUDE.md` ระบุว่า `/import/wizard` และ `/synonyms` ถูกลบไปแล้ว)

## คำถามที่ใหญ่กว่าตัวไฟล์

**อัปเดตจากการปิด[ตั๋ว 01](01-precedence-order.md) (2026-09-06):** ทางเข้าฝั่ง**เอเจนต์**ตอบไปแล้วครึ่งหนึ่ง — คือ `AGENTS.md` (Antigravity อ่านตรง, Claude Code อ่านผ่าน `CLAUDE.md` ที่ import) ตั๋วนี้จึงเหลือแค่คำถามฝั่ง**คน**: เปิดรีโปครั้งแรกต้องอ่านอะไร ระหว่าง `START_HERE.md`, `README.md`, `docs/INDEX.md` — ทั้งสามยังแข่งกันเป็น "ทางเข้า" อยู่ และต้องเหลือเส้นทางเดียว

## เกณฑ์ว่าตอบครบ

- [ ] ตัดสินชะตากรรมของ `START_HERE.md` แล้ว
- [ ] รู้ว่าทางเข้าสำหรับ **คน** คือไฟล์ไหน และทางเข้าสำหรับ **เอเจนต์** คือไฟล์ไหน
- [ ] ถ้าเขียนใหม่ ทุกคำสั่งในนั้นถูกรันจริงแล้วว่าใช้ได้ ไม่ใช่แค่ดูแล้วน่าจะถูก
- [ ] ไม่มีทางเข้าที่สี่หลงเหลือให้คนหลงไปอ่าน
